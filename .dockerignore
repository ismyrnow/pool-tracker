# The Dockerfile copies only what it needs (allowlist), so this is a
# second layer of defense that also shrinks the build context.
node_modules
dist
data
.env
.env.*
.git
.gitignore
.claude
e2e
docs
scripts
*.md
components.json
.DS_Store
