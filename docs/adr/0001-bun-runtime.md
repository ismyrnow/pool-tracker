# Bun as JavaScript Runtime

Bun provides native TypeScript execution, a built-in HTTP server (`Bun.serve`), and a first-class SQLite driver (`bun:sqlite`) in a single binary. Choosing it over Node.js eliminates the need for a TypeScript transpiler, a separate HTTP framework, and a SQLite adapter package.
