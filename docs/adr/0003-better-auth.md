# better-auth for Authentication

better-auth manages sessions, HTTP-only cookies, and its own schema tables inside the same SQLite database. This keeps the stack entirely self-contained — no third-party auth service, no separate session store, no additional infrastructure to operate.
